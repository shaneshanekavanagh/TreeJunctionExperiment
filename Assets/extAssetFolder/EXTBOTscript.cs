using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EXTBOTscript : MonoBehaviour {

    Transform parentTargetPos;
    public GameObject budObjPre;
    public GameObject extObjPre;
    // Use this for initialization
    void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}

    public void GotHit()
    {
        //sprout or split function
        //if sprout get in the same array as siblings
        //if split, close array?
        //is the array on this object meaning it's on all siblings too?
        //are siblings in an inbuilt array?
    }
}
